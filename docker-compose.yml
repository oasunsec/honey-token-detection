services:
  canary:
    build: .
    ports:
      - "8000:8000"
    environment:
      CANARY_DB_PATH: /data/canary.db
      CANARY_BASE_URL: http://localhost:8000
      CANARY_ALERT_MODE: console
    volumes:
      - canary-data:/data
volumes:
  canary-data:
