# Local Word callback

[GIF preview](local-word-callback.gif) · [MP4 recording](local-word-callback.mp4)

Recorded on 7 September 2026 with Word 16, a loopback FastAPI receiver, SQLite, and console alerts. The synthetic document produced a callback at `2026-09-07T21:03:31.811633+00:00`; the stored User-Agent identified Microsoft Office 16, severity was `high`, and console-alert status was `sent`.

The clip combines the CLI creation step, the opened document, and the stored event from the same local run. The Word account area, cursor, callback token, addresses, paths, and personal details are excluded.

This recording covers the local Word path. The Azure-to-Sentinel path is documented in the [case study](../../CASE_STUDY.md).
